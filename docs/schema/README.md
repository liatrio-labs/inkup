# Moved

The Session file format's JSON Schema is now [`contract/session.schema.json`](../../contract/session.schema.json),
next to the wire protocol's schema and the shared fixtures. See [ADR 0007](../adr/0007-central-contract.md).
