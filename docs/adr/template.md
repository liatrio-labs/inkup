---
status: proposed
date: YYYY-MM-DD
---

<!-- Copy to docs/adr/NNNN-short-slug.md with the next free number, then add it to the index in README.md.
     Status: proposed, accepted, or superseded. A new ADR that replaces one adds `supersedes: NNNN` above;
     the old one then gets `status: superseded` and `superseded-by: MMMM`. -->

# The decision, stated as a sentence a reader can act on

The context: what forces the decision, and what is true that makes it hard. Then the decision itself.

**A rule, in bold.** What it says, where it lives in the code (`path/to/file.ts`, `identifierName`), and why.

**Another rule.** Keep each one to what constrains future code. Leave implementation detail to the pull request.

## Considered options

- An alternative we did not take: why not, in a sentence or two.

## Consequences

- What this makes easier, harder, or necessary elsewhere.

## History

- YYYY-MM-DD: we first thought X. YYYY-MM-DD: now Y, because Z.

## Sources

- Links to docs, specs or issues the decision relies on.
