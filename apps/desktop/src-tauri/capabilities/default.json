{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core events only. Host data comes through the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
